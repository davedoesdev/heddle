test:
  override:
    - .circle-ci/build.sh
general:
  artifacts:
    - "/heddle-*.tar.xz"
    - "/heddle-*-log-*.xz"
    - "/heddle-*-src-*.xz"
dependencies:
  cache_directories:
    - downloads
  pre:
    - .circle-ci/deps.sh
