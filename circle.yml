test:
  override:
    - .circle-ci/build.sh
general:
  artifacts:
    - "/heddle-*.tar.xz"
    - "/heddle-*-log-*.txt.xz"
dependencies:
  cache_directories:
    - downloads
  pre:
    - .circle-ci/deps.sh
