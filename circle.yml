test:
  override:
    - .circle-ci/build.sh
general:
  artifacts:
    - "/heddle-*.tar.xz"
    - "/heddle-*-log-*.txt.xz"
    - "/heddle-*-src-*.tar"
machine:
  node:
    version: 0.12
dependencies:
  cache_directories:
    - downloads
  pre:
    - .circle-ci/deps.sh
