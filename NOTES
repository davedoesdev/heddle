In aboriginal-1.2.9:

/path/to/heddle/aboriginal_scripts/config.sh
./build.sh x86_64
/path/to/heddle/image_scripts/make_build_and_home_images.sh
/path/to/heddle/aboriginal_scripts/build_heddle.sh
/path/to/heddle/image_scripts/make_run_and_extra_images.sh
/path/to/heddle/aboriginal_scripts/run_heddle.sh
/path/to/heddle/image_scripts/make_dist_image.sh
/path/to/heddle/aboriginal_scripts/dist_heddle.sh
/path/to/heddle/image_scripts/boot_heddle.sh

---

# Wget with HTTPS support
wget ftp://ftp.gnu.org/gnu/wget/wget-1.15.tar.gz
tar xf wget-1.15.tar.gz
rm -f wget-1.15.tar.gz
cd wget-1.15
./configure --with-ssl=openssl --with-libssl-prefix=/usr/ssl
make
make install
cd ..
rm -rf wget-1.15

# pip
wget --no-check-certificate https://bootstrap.pypa.io/get-pip.py
python get-pip.py
rm get-pip.py

#TODO
# boot in graphic mode, get logins on vts, console and ttyS0
# resize image file
#   Resize to disk when boots
# test on bare metal
# patch init.sh outside qemu so doesn't try to mount hdb and hdc
# Image upgrade
#   Use salt to update image itself. 
# Networking: dhcp on metal

# nfdhcpd
# kvm
# capstan
# Fold
# Saltstack

